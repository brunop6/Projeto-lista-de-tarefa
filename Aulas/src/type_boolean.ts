let estaAutenticado: boolean = true;

//estaAutenticado = false;

let codeStatus: string = '';

//Diferente de 0, string vazia, undefined = verdadeiro
estaAutenticado = Boolean(codeStatus);

console.log(estaAutenticado);